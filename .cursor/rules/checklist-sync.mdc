---
description: 主方案 §十 验收与 §八§九 同步（流程见 base.mdc §5）
alwaysApply: true
---

# 方案与验收同步

**入口**：**`base.mdc` → 5. 收尾**。真源：**`docs/GroomerFlow方案.md`**（§八、§九、§十）。  
**风格方案**同步：**`user-preferences.mdc` §5**（不在此重复）。

## 勾选 `[x]`

- §十项达到可重复验证的定义；**同轮**改勾选。  
- §八 / §九 与实现不一致 → **简短**修订。  
- **禁止**未 lint/build（非环境阻断）即勾选。

## 反勾选 `[ ]`

- 功能移除或代码回退 → **同轮** `[x]`→`[ ]`，一行注明原因。

## 须先问用户

- 扩大 MVP（§三.2/§三.3）、改定价、换技术栈。

## 依赖提交

- `package.json` 与 `package-lock.json` **同提交**；commit 摘要须真实（禁占位符）。

## 不得提前勾 §十

- UI mock 无持久化；无 RLS；`/book` 不可用；依赖主人登录自助端（Phase 2）。
