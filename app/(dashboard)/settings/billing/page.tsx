import { Card, CardDescription, CardTitle } from "@/components/ui/card";

export default function BillingPlaceholderPage() {
  return (
    <Card>
      <CardTitle>Billing</CardTitle>
      <CardDescription>
        Lemon Squeezy subscriptions will be available in a future release. Your
        trial includes all Starter features during MVP.
      </CardDescription>
    </Card>
  );
}
